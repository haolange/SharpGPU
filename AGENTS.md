# SharpGPU

Product rules are in DESIGN.md. Verification commands are owned by docs/VERIFICATION.md.
Work on the checked-out main branch; do not create branches or PRs without explicit authorization.
Preserve unrelated work. No legacy aliases, forwarding assemblies, silent dependency fallbacks or duplicate implementation paths.
Hand-written C# uses block namespaces, Allman braces, four spaces, m_PascalCase fields and s_PascalCase static fields. Generated and native binding conventions remain explicit exceptions.
Use current build/test/runtime evidence. Unavailable matching platforms are TODO(UNVERIFIED), never PASS.
